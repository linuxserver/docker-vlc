---

# jenkins variables
project_name: docker-vlc
external_type: na
custom_version_command: "curl -sL https://mirror.rackspace.com/archlinux/extra/os/x86_64/extra.db | tar tzf - | awk -F '(-|/)' '/vlc/ {print $2; exit}'"
release_type: stable
release_tag: latest
ls_branch: master
repo_vars:
  - BUILD_VERSION_ARG = 'VLC_VERSION'
  - LS_USER = 'linuxserver'
  - LS_REPO = 'docker-vlc'
  - CONTAINER_NAME = 'vlc'
  - DOCKERHUB_IMAGE = 'linuxserver/vlc'
  - DEV_DOCKERHUB_IMAGE = 'lsiodev/vlc'
  - PR_DOCKERHUB_IMAGE = 'lspipepr/vlc'
  - DIST_IMAGE = 'arch'
  - MULTIARCH = 'true'
  - CI = 'true'
  - CI_WEB = 'true'
  - CI_PORT = '3001'
  - CI_SSL = 'true'
  - CI_DELAY = '120'
  - CI_DOCKERENV = 'TZ=US/Pacific'
  - CI_AUTH = 'user:password'
  - CI_WEBPATH = ''
